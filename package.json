{
  "name": "deusvault",
  "version": "1.0.0",
  "description": "Веб-приложение для систематизации личных коллекций (DeusVault)",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix server run start:dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "lint-all": "npm --prefix client run lint && npm --prefix server run lint",
    "format-all": "npm --prefix client run format && npm --prefix server run format",
    "type-check-all": "npm --prefix client run type-check && npm --prefix server run type-check"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DivarPG/DeusVault.git"
  },
  "author": "Бредихина А. С., Цехмистрова Д. А.",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "bootstrap": "^5.3.8",
    "concurrently": "^8.2.2",
    "husky": "^8.0.0",
    "lint-staged": "^16.4.0",
    "react-bootstrap": "^2.10.10"
  },
  "lint-staged": {
    "client/**/*.{ts,tsx,js,jsx}": [
      "npm --prefix client run lint"
    ],
    "server/**/*.ts": [
      "npm --prefix server run lint"
    ]
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  }
}
